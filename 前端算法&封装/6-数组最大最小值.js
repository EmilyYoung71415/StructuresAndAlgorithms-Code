/**
 * Math.max.apply(null,arr)
 * Math.max(...arr)
 * 
 * 
 * reduce:
 *      
        function max(prev, next) {
            return Math.max(prev, next);
        }
 *      console.log(arr.reduce(max));
 */